<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.madayV3.blog.mapper.PlanMapper">

    <select id="findAll" resultType="com.madayV3.blog.dto.PlanDto">
        SELECT id, plan_name AS planName, monthly_fee AS monthlyFee,
               data_gb AS dataGb, call_min AS callMin, description
        FROM plan ORDER BY monthly_fee ASC
    </select>

    <select id="findById" resultType="com.madayV3.blog.dto.PlanDto">
        SELECT id, plan_name AS planName, monthly_fee AS monthlyFee,
               data_gb AS dataGb, call_min AS callMin, description
        FROM plan WHERE id = #{id}
    </select>

</mapper>
